<div class="lifecard mb-1 my-3">
<h2 class="page-main-header"></h2>
<%= link_to life.name, life_path(life.id)  %><br>
    <%= life.details %>
     <% if life.photo.attached? %>
    <%= cl_image_tag life.photo.key, height:200, width:80, crop: :fill %>
  <% else %>
     <p>🚨 There is no image attached</p>
    <% end %>
</div>
